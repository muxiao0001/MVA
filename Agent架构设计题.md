# Agent 架构设计笔试



## 模块一：Context / Performance



### 选题 2：一个 Session 连续聊了 200 轮，Context 快爆了，如何压缩？如何保证压缩后的对话仍然流畅？



轮数不能作为压缩依据，因为轮数并不能准确代表上下文大小。200 轮短问短答可能仍然很小，而包含网页、代码、图片和终端日志的几十轮会话就可能接近窗口上限。更合理的做法是在每次模型调用前计算实际渲染后的 Token，包括系统提示、工具定义、历史消息、附件描述以及预计输出，并提前预留模型回复和下一次工具结果所需空间。当输入占可用预算约六到七成时，可以在后台预压缩；接近八成时，应在当前轮强制压缩。如果一直等到模型返回 context overflow 才处理，用户会额外承受一次失败调用和重试延迟。OpenAI 当前的 [Compaction 文档](https://developers.openai.com/api/docs/guides/compaction) 也采用 Token 阈值触发，而不是按对话轮数触发。

我会设计一个“滑动窗口 + 结构化摘要 + 按需回溯”的上下文压缩机制，而不是简单删除最早的消息。首先，系统持续监控 Token 使用量，例如达到上下文上限的 70%～80% 时触发压缩。压缩时保留系统指令、当前任务以及最近若干轮对话原文，因为最近对话包含大量指代关系，例如“这个方案”“刚才那段代码”。较早的对话则转换为结构化的 Session 状态，重点保留用户目标、关键事实、明确约束、已经做出的决定、用户偏好、失败尝试、待办事项、未解决问题，以及代码和文件等产物的位置。

为了保证对话仍然流畅，一方面要保留最近对话原文，维持代词、语气和局部逻辑的连续性；另一方面要专门记录尚未结束的话题和用户已经确认的决定，避免 Agent 重复询问或者推翻之前的结论。当用户提到“之前那个方案”时，系统先根据当前问题从历史对话中召回对应原文，再交给模型回答，而不是只依赖可能丢失细节的摘要。

原始聊天记录也不应该真正删除，而是存放在上下文之外的数据库中，并建立关键词和向量索引。摘要只负责提供全局状态，原始记录负责在需要时恢复细节。对于数字、接口参数、代码、文件路径等不能被模糊概括的信息，最好原样保存，并在摘要中记录其来源消息 ID。

此外，还要避免摘要被反复压缩后产生信息漂移。可以为每次摘要保存版本和来源范围，定期根据原始消息重新生成摘要，并检查新摘要是否遗漏关键约束、待办事项和用户确认过的结论。如果系统对召回结果不确定，应向用户确认，而不能依靠摘要自行补全。



## 模块二：Memory



### 选题 1：和聊天 Agent 熟悉半个月后，用户问了一个以前问过的问题，Agent 如何合理召回？



如果用户在半个月后再次问到以前的问题，我会通过“分层记忆和按需召回”来处理，而不是每次都把全部历史聊天记录交给大模型。系统可以将记忆分为当前会话的短期记忆、过去具体问答形成的情景记忆，以及从长期交流中提炼出的用户偏好和稳定信息。对于有价值的历史对话，还需要记录当时的问题背景、解决方案、用户反馈和最终结果，而不只是保存原始问句。

当用户提出新问题后，Agent 首先判断这个问题是否需要参考历史。如果需要，就结合当前上下文改写检索查询，再通过向量检索、关键词检索和实体匹配，从历史记忆中召回候选内容，并根据语义相关性、时间、重要程度以及用户是否验证过该方案进行重排序。半个月前的记忆虽然时间权重有所降低，但如果和当前问题高度相关，仍然应该被优先召回。

召回以后也不能直接复制旧答案，还要判断用户当前的需求和之前是否一致，以及旧方案是否已经过期、是否曾被用户反馈为无效。如果历史记忆的置信度较高，Agent 可以自然地告诉用户：“你之前问过类似问题，当时的处理方式是……”。如果只能模糊匹配，就将其作为回答参考，或者进一步向用户确认。这样能够让 Agent 保持对话连续性，同时避免错误关联和机械重复。

从工程实现上，我会把这个过程设计为 RecallGate、记忆检索、重排序、冲突校验、回答生成和记忆更新等节点。它本质上可以看作面向个人历史的 RAG，但相比普通知识库 RAG，还需要重点处理用户隔离、时间因素、反馈结果、记忆冲突和动态更新。核心目标不是找出文字最相似的旧聊天，而是召回对当前问题真正有帮助的历史经验。



## 模块三：Task



### 选题 2：用户要求 Agent 每天早上 9 点根据昨天聊天情况做复盘总结，如何设计？



可以把它设计成一个“定时触发的对话复盘工作流”，而不是依赖 Agent 自己在对话中记住时间。首先，需要确认用户所在时区、复盘范围和发送渠道。例如按照用户本地时间，每天早上 9 点触发任务，复盘“昨天 00:00—24:00”的聊天。定时任务可以由 Cron、任务调度平台或消息队列触发。

任务触发后，系统根据 `user_id` 和时间范围查询昨天的聊天记录，并进行预处理，例如过滤系统消息、工具调用日志和无效内容，再按照话题或会话进行分组。如果聊天记录较长，可以先分别总结每段会话，再对多个局部总结进行二次汇总，形成分层总结，避免一次性输入过多内容导致上下文超限。

总结内容最好采用固定结构，包括：昨天讨论了什么、形成了哪些结论或决定、完成了哪些事项、还有哪些问题未解决，以及今天建议继续做什么。Agent 还可以结合用户的长期目标进行判断，例如用户正在准备 AI Agent 岗位，就可以指出昨天学习的内容与求职准备之间的关系。但长期记忆只作为背景，不能把更早的聊天误写进“昨天发生的事情”。

生成完成后，系统将总结发送给用户，并将其保存为一条结构化的“每日复盘记忆”。为了防止重复发送，可以使用“用户 ID + 日期 + 任务类型”作为幂等键；如果生成或发送失败，则通过任务队列进行有限次数重试并记录日志。如果昨天没有聊天，可以选择不发送，或者发送一条简短提示，这应该由用户提前配置。



## 模块四：Tool / Session Runtime



### 选题 1：异步工具不能让用户一直等，但结果依然重要，如何设计异步执行和完成通知？



对于耗时较长的工具，我会采用“任务提交与结果消费分离”的异步架构，避免 Agent 一直占用当前对话等待结果。用户发起请求后，Agent 先完成参数校验，将任务写入持久化任务队列，并立即返回“任务已经开始处理”的确认信息，同时生成唯一的 `job_id`。当前对话可以继续进行，用户不需要停留在原页面等待。

任务中不仅要保存工具参数，还要保存后续恢复对话所需的信息，后台 Worker 从消息队列获取任务并执行工具。执行过程中需要设置超时、有限次数重试和幂等键。即使消息被重复消费，同一个任务也只能产生一次有效结果。对于可以展示进度的任务，可以更新阶段状态，例如“正在下载数据”“正在生成报告”，但要控制通知频率，避免频繁打扰用户。

工具完成后，将结果持久化，并发送 `TaskCompleted` 事件。通知服务消费该事件，根据 `job_id` 找到原始用户、Session 和请求上下文，再生成一条有语义的完成通知，而不是只显示“任务成功”。例如：“你刚才提交的论文分析任务已经完成，共处理了 32 篇论文，报告可以在这里查看。”

如果用户仍在原 Session 中，可以通过 WebSocket 或 SSE 实时插入结果；如果用户已经离开，则通过应用内通知、邮件或用户指定渠道提醒。用户重新进入对话时，Agent 也可以查询该 Session 下尚未确认的任务结果并主动展示。

为了保证结果回来后仍能正确衔接对话，任务创建时必须保存“续接上下文”，包括原始意图、关键参数、消息 ID和结果应该用于哪个后续步骤。结果返回后，系统先检查用户是否已经取消任务、需求是否发生变化，以及结果是否过期，再决定继续执行工作流、生成通知，还是仅保存结果供用户查看。



## 模块五：Agent Runtime 架构对比



### 选题 1：Claude Code 的工具输出方式和国内 GLM / 豆包等 OpenAI-compatible function calling 有什么不同？他们各自这样设计的优缺点是什么？

工具输出方式不同主要是 Anthropic Messages 协议与 OpenAI Chat Completions 兼容协议的区别。

Claude Code 底层主要采用 Anthropic 的工具调用协议，而 GLM、豆包等模型通常提供 OpenAI-compatible 的 Function Calling 接口。两者的基本流程相同，都是模型先发起工具调用，由 Agent 执行工具，再把结果返回给模型继续推理，主要区别在于消息结构。Claude 将文本、工具调用和工具结果都看作内容块：模型通过 Assistant 消息中的 `tool_use` 发起调用，参数通常是结构化 JSON 对象；工具执行完成后，Agent 在 User 消息中通过 `tool_result` 返回结果，并使用 `tool_use_id` 建立对应关系。工具结果还可以包含文本、图片、文档等内容，并通过 `is_error` 明确表示执行是否失败。

OpenAI-compatible 接口则是在传统的角色消息结构上扩展工具调用。模型通过 Assistant 消息中的 `tool_calls` 发起调用，函数参数通常以 JSON 字符串返回；Agent 执行完成后，再增加一条 `role: tool` 的消息，并通过 `tool_call_id` 关联原来的调用。它的优势是协议直观、SDK 和框架生态成熟，GLM、豆包等模型可以较容易接入 LangChain、LangGraph 等现有系统，也方便在不同模型之间切换。但它的函数参数需要额外进行 JSON 解析，流式调用时还可能收到不完整的参数分片，而且不同厂商在并行调用、工具选择和错误格式方面不一定完全一致。

相比之下，Anthropic 的 Content Block 设计结构化程度更高，天然适合多模态工具结果、错误表达和复杂 Agent 工作流，因此比较适合 Claude Code 这种需要频繁读取文件、修改代码、执行命令和调用 MCP 工具的场景。它的缺点是协议更复杂、消息顺序要求更严格，并且与 OpenAI 生态之间需要额外的适配层。总体来说，Claude 的设计更强调工具交互的表达能力和 Agent 原生性，而 OpenAI-compatible 的设计更强调接口统一、生态兼容和较低的接入成本。

