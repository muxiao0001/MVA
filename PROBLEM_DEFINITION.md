# 最小可用 Agent：可执行问题定义

## 1. 最终交付物

- 一个可运行的最小 Agent：接收输入，自主决定回复或调用工具，并循环至给出最终答案。
- 至少 3 个已注册工具：`calculator`、`search`（可 mock）及 1 个自定义工具。
- session 与 context 管理、基础 context 压缩、异常处理、工具调用 trace/日志。
- 覆盖上述能力的测试用例。
- 代码链接、终端或网页操作录屏、README、AI Prompt 与问题解决记录。

## 2. 硬约束

- 主流程不得依赖现有 Agent 框架（如 LangGraph、OpenHands、OpenClaw）；核心 Agent Runtime 必须自行实现。
- 必须调用真实 LLM API。
- 每个工具必须包含名称、描述、参数 Schema；LLM 必须能基于 Schema 自主选择调用。
- 必须解析 LLM 输出，识别工具调用或最终答案；原文还要求提取“思考过程”。
- Agent 循环必须有最大轮次限制。
- 同一用户的不同窗口必须是相互隔离、可分别续聊的 session。
- 必须支持普通追问及需要继续调用工具的追问，并保留此前状态。

## 3. 隐含要求

- Agent 不能仅演示 happy path：无效参数、工具失败、LLM 输出不合规等情况应可控失败。
- session 标识、消息、工具结果等状态需有明确归属，避免串话。
- trace/日志应足以还原一次工具调用过程，同时避免泄露密钥等敏感信息。
- README 应让评审者能够独立运行并理解系统边界及 memory 的召回与放置时机。
- 测试应验证行为结果及隔离性，而非只验证单个函数。
- 使用CLI进行交互，使用python技术栈，只要求进行本地部署，不使用测试框架，可以使用测试用例。
- session 要求跨进程重启持久化，原文明确了“可随时接着聊”。

## 4. 什么叫完成

- 可用真实 LLM API 跑通“直接回答”和“调用工具后回答”两条完整链路。
- 3 个以上工具可通过统一注册信息被 LLM 自主选择并正确执行。
- 循环能依据工具结果继续或结束，且不会超过最大轮次。
- 两个 session 可交错对话、分别续聊，历史与状态不互相污染。
- 长对话可触发基础压缩，压缩后仍能完成普通追问和工具追问。
- 典型异常有明确结果，工具调用可追踪。
- 自动化测试通过，且全部提交材料齐全、按 README 可复现。

## 5. 最容易翻车的风险点

- 借助库时越过“核心 Runtime 自行实现”的边界。
- session 只做表面区分，历史、工具状态或待办数据仍发生串话。
- LLM 输出稍有偏差就无法解析，导致循环卡死或错误调用工具。
- context 只截断不保留关键状态，压缩后追问失效。
- 最大轮次、异常分支与 trace 未真正进入主循环。
- mock search 与“真实 LLM API”被混为一谈；允许 mock 的仅明确指向 search 工具。
- 演示能跑但缺少可复现配置、测试或要求中的提交材料。
