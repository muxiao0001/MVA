# 《最小可用 Agent》可行方案评估

> 目标：优先稳定交付，避免为展示效果引入不必要复杂度。
> 前提：遵守 `TASK.md`、`PROBLEM_DEFINITION.md` 与 `REQUIREMENTS_ANALYSIS.md` 中已确认的全部约束。

## 1. 共同约束

三条方案均满足：

- Python CLI、本地运行；
- 默认调用 `deepseek-v4-pro`，使用 OpenAI 兼容协议并开启 thinking；
- Agent loop、工具调度、session、context 和 trace 自行实现；
- 工具为 `calculator`、mock `search`、session 级 `todo`；
- session 跨进程恢复；
- 原始 `reasoning_content` 仅保留在必要的内部协议状态，不展示、不进入普通日志；
- 不使用 Agent 框架；
- 不使用测试框架，以可执行验收脚本和真实 API 冒烟测试验证。

使用 OpenAI Python SDK 仅作为 HTTP/API 协议客户端，不属于借用 Agent Runtime。

## 2. 方案总览

| 方案 | 核心选择 | 实现复杂度 | 可测试性 | 稳定交付判断 |
|---|---|---:|---:|---|
| A. 原生 Tool Calls + SQLite 分层状态 | 使用 DeepSeek 原生 `tool_calls`，自建有限循环；SQLite 保存 session、消息、todo 和 trace | 中 | 高 | **最佳** |
| B. 自定义 ReAct JSON + 文件存储 | 要求模型输出自定义 action JSON；每个 session 使用 JSON/JSONL 文件 | 低到中 | 中 | 初期快，后期容易脆 |
| C. 事件溯源 Runtime + SQLite | 每次模型、工具和状态变化都记为事件，再由事件还原 session | 高 | 很高 | 能力最强，但明显过度设计 |

## 3. 方案 A：原生 Tool Calls + SQLite 分层状态

### 3.1 大致怎么做

将系统分为六个明确职责：

1. **CLI：**接收输入、选择或创建 session、展示答案和决策摘要。
2. **Model Adapter：**调用 DeepSeek Chat Completions，保留 `content`、`tool_calls`、`reasoning_content` 和结束原因。
3. **Agent Runtime：**自行执行有限循环，判断直接回答、工具调用、继续或异常结束。
4. **Tool Registry：**统一注册工具名称、描述、JSON Schema 与执行入口，调用前完成参数校验。
5. **Session/Context：**SQLite 持久化完整 session 状态；发给模型的 context 使用“历史摘要 + 最近完整消息组 + 当前输入”。
6. **Trace：**记录 run、步骤、工具状态、耗时和终止原因；与内部 `reasoning_content` 分开存放。

SQLite 中逻辑上区分 session 元数据、消息/内部协议状态、todo 业务数据和 trace。具体表结构不在本方案阶段锁定。

Context 超过阈值时生成滚动摘要，同时保留最近若干个完整交互组；工具调用与工具结果必须整体保留或整体压缩。todo 始终由独立业务状态提供，不依赖模型从历史中“记住”。

### 3.2 主要优点

- 直接使用 DeepSeek 官方工具调用协议，减少自定义输出格式造成的解析失败。
- SQLite 具备事务和单文件持久化能力，比手写 JSON 覆盖更不容易损坏。
- session、todo、context、trace 边界清楚，便于证明隔离和隐私要求。
- 核心循环仍完全可见、可讲解，符合“Runtime 自行实现”。
- 依赖少，本地运行和录屏演示都较稳定。
- 将完整记录与发送给模型的 context 分开，压缩失败不会直接丢失原始业务状态。

### 3.3 主要风险

- DeepSeek thinking + tool call 要求正确续传 `reasoning_content`；消息组装错误可能产生 400。
- SQLite 状态边界设计不清时，仍可能出现跨 session 查询或部分写入。
- LLM 生成摘要存在非确定性，需用固定验收事实验证压缩质量。
- 若同时支持多个 CLI 进程写同一 session，需要额外处理竞争；生产级并发不属于当前 P0。

### 3.4 实现复杂度

**中等。**

主要复杂度集中在：

- 有限循环和终止状态；
- DeepSeek 消息续传；
- 工具调用与结果配对；
- SQLite 持久化边界；
- context 压缩后的消息合法性。

这些工作均直接对应题目核心能力，没有明显的非必要模块。

### 3.5 可测试性

**高。**

- Model Adapter 可替换为固定响应源，稳定构造直接回答、单工具、多工具、畸形参数和死循环。
- 每个测试 session 使用独立临时 SQLite 文件，可验证重启恢复与隔离。
- Tool Registry 可独立验证 Schema、未知工具和执行异常。
- Context Builder 可直接检查压缩前后关键事实及 tool call/result 配对。
- 少量真实 DeepSeek API 用例负责验证协议兼容，其余用例无需依赖模型随机性。

## 4. 方案 B：自定义 ReAct JSON + 文件存储

### 4.1 大致怎么做

不依赖模型原生 `tool_calls` 作为主要决策输出，而是在 Prompt 中要求每一轮返回固定 JSON：

- 直接回答；
- 调用某个工具及参数；
- 继续循环；
- 最终答案；
- 可公开的决策摘要。

Runtime 解析该 JSON 后调度工具。每个 session 使用独立 JSON 或 JSONL 文件保存消息、摘要、todo 和 trace；context 采用最近消息截取加摘要。

### 4.2 主要优点

- 概念直观，评审者容易看到“模型输出—解析—动作”的完整过程。
- 初期依赖和存储结构较少，适合快速做出单 session 演示。
- 可以完全控制公开决策摘要的字段格式。
- 文件内容便于人工检查和录屏展示。

### 4.3 主要风险

- 模型可能输出 Markdown、解释文字、截断 JSON 或不符合约定的字段，解析稳定性弱。
- thinking 模式仍有独立的 `reasoning_content` 协议要求，自定义 JSON 并不能消除 DeepSeek 上下文处理。
- 绕开原生 Tool Calls 后，需要自行解决更多格式纠错和重试问题。
- JSON 文件覆盖写入遇到中断可能损坏；多个 CLI 实例更容易出现覆盖或丢数据。
- 消息、todo 和 trace 混在文件中，后续隔离、脱敏和压缩容易互相影响。

### 4.4 实现复杂度

**低到中等。**

Happy path 较简单，但为了达到当前验收要求，还需补充：

- JSON 提取和修复；
- 原子写入；
- 文件损坏恢复；
- 重复 todo 防护；
- 工具消息配对；
- 格式错误重试上限。

这些补丁会逐渐抵消最初的简单优势。

### 4.5 可测试性

**中等。**

- 自定义 JSON 解析器容易用固定样例测试。
- 文件状态可直接检查，session 隔离用例较直观。
- 但真实模型是否稳定服从 JSON Prompt 具有波动性，真实 API 测试更容易偶发失败。
- 文件中断、重复写和恢复用例需要额外构造，且结果容易依赖文件操作时机。

## 5. 方案 C：事件溯源 Runtime + SQLite

### 5.1 大致怎么做

把每次状态变化都保存为不可变事件，例如：

- 用户输入；
- 模型请求与响应；
- 工具调用、成功或失败；
- todo 变更；
- context 压缩；
- run 结束。

当前 session 状态由事件顺序还原，并定期生成 checkpoint 加快恢复。Agent 仍使用 DeepSeek 原生 `tool_calls`，但循环、恢复、trace 和业务状态都围绕事件流组织。

### 5.2 主要优点

- 审计和 trace 最完整，可以重放任意一次 run。
- 中途失败后更容易判断已完成到哪一步。
- 重复副作用、状态恢复和错误定位有清晰依据。
- 测试可从固定事件序列还原状态，确定性最高。
- 如果未来扩展长任务或多 Agent，演进空间最大。

### 5.3 主要风险

- 需要定义事件版本、状态归并、checkpoint、一致性和迁移规则。
- context 压缩改变“模型可见状态”但不改变事件历史，概念更难解释。
- 需要处理事件已写入但副作用未完成、或副作用完成但事件未确认等边界。
- 代码量和调试面显著增加，容易挤压测试、README 和录屏准备时间。
- 对单机最小 Agent 来说，大量能力不会进入实际验收。

### 5.4 实现复杂度

**高。**

除方案 A 的全部核心工作外，还要实现事件模型、reducer、checkpoint、重放与版本兼容。它更接近一个可扩展 Runtime 基础设施，而不是本题需要的 MVP。

### 5.5 可测试性

**很高，但测试成本也高。**

- 事件序列、状态还原和失败恢复可以精确测试。
- trace 天然完整，适合故障注入和重放。
- 但必须为事件顺序、重复事件、checkpoint 和迁移增加大量用例。
- 在“不使用测试框架”的限制下，维护这些测试脚本本身也会变成负担。

## 6. 推荐结论

### 推荐方案 A：原生 Tool Calls + SQLite 分层状态

推荐原因：

1. **最贴合官方协议。** DeepSeek V4 Pro 已原生支持 Tool Calls，直接使用能减少输出格式漂移。
2. **最贴合题目验收。** Agent loop、Schema、session、context、trace 都由项目自身完成且容易展示。
3. **持久化更稳。** SQLite 比手写 JSON 文件更适合跨进程恢复、隔离和失败保护，同时仍保持本地单文件部署。
4. **测试确定性较好。** 模型适配层和持久化层可分别替换，绝大多数异常用例不必依赖真实模型随机输出。
5. **复杂度可控。** 所有中等复杂度工作都直接服务于 P0，不需要先建设通用基础设施。

### 为什么不选方案 B

- 它看似最简单，但把稳定性交给 Prompt 格式服从度。
- DeepSeek 已提供原生 Tool Calls，自定义动作 JSON 属于重复造协议。
- 为处理畸形 JSON、原子写入和损坏恢复追加的补丁，会让最终结构比方案 A 更脆、更难解释。

方案 B 只适合一次性 demo，不适合当前要求的跨进程 session、压缩、异常和完整验收。

### 为什么不选方案 C

- 它的审计和恢复能力最好，但当前题目不要求事件重放、长任务恢复或多 Agent。
- 事件模型、checkpoint 和 reducer 会显著增加代码与测试面。
- 在交付优先的前提下，这些投入更可能拖累核心功能、README 与录屏质量。

方案 C 更适合作为方案 A 通过验收后的演进方向，不适合作为首版。

## 7. 最终决策

采用方案 A，并坚持以下收敛原则：

- 只实现单 Agent 有限循环；
- 使用 DeepSeek 原生 Tool Calls，不创造第二套模型动作协议；
- SQLite 只承担 P0 所需的 session、内部消息、todo 与 trace；
- context 只做基础滚动摘要和最近完整消息保留；
- 优先完成确定性验收脚本，再补真实 API 冒烟用例；
- 不提前加入 Web、多 Agent、向量记忆、事件溯源或生产级并发。
